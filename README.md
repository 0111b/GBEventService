# Mock for the NODE API: GBEvents 

To run service:

> bundle exec ruby service.rb

Endpoints:

- GET  / - list of the accepted events
- POST / - simulated endpoint

Logs:

- history.json - valid events
- error.log - errors
